export type Encantamento = {
  rarity: string;
  item: string;
  group: string;
  description: string;
};

export type Facet = {
  name: string;
  upside: string;
  downside: string;
  description: string;
};
